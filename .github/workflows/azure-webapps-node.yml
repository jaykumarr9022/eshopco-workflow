name: Example Workflow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 24f2004977@ds.study.iitm.ac.in
        run: echo "Hello, world!"
